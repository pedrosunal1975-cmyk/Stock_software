# ESEF overlay: interest_expense
# Only fields that differ from base definition
component_id: interest_expense
matching_rules:
  local_name_rules:
    - patterns:
        - "FinanceCosts"
        - "InterestExpense"
      match_type: exact
      weight: 15
    - patterns:
        - "FinanceCost"
        - "InterestExpense"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Finance Costs"
        - "Interest Expense"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
