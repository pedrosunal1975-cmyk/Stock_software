# ESEF overlay: operating_income
# Only fields that differ from base definition
component_id: operating_income
matching_rules:
  local_name_rules:
    - patterns:
        - "ProfitLossFromOperatingActivities"
        - "OperatingProfit"
      match_type: exact
      weight: 15
    - patterns:
        - "OperatingProfit"
        - "ProfitFromOperating"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Operating Profit"
        - "Profit from Operating Activities"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
