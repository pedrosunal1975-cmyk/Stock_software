# ESEF overlay: income_before_tax
# Only fields that differ from base definition
component_id: income_before_tax
matching_rules:
  local_name_rules:
    - patterns:
        - "ProfitLossBeforeTax"
      match_type: exact
      weight: 15
    - patterns:
        - "ProfitBeforeTax"
        - "ProfitLossBeforeTax"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Profit Before Tax"
        - "Profit (Loss) Before Tax"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
