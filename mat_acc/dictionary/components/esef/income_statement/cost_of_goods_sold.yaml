# ESEF overlay: cost_of_goods_sold
# Only fields that differ from base definition
component_id: cost_of_goods_sold
matching_rules:
  local_name_rules:
    - patterns:
        - "CostOfSales"
      match_type: exact
      weight: 15
    - patterns:
        - "CostOfSales"
        - "CostOfGoodsSold"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Cost of Sales"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
