# ESEF overlay: accounts_payable
# Only fields that differ from base definition
component_id: accounts_payable
matching_rules:
  local_name_rules:
    - patterns:
        - "TradeAndOtherCurrentPayables"
        - "TradePayables"
      match_type: exact
      weight: 15
    - patterns:
        - "TradePayable"
        - "CurrentTradePayables"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Trade and Other Current Payables"
        - "Trade Payables"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
