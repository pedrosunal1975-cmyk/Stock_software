# ESEF overlay: noncurrent_liabilities
# IFRS uses NoncurrentLiabilities as standard concept name
component_id: noncurrent_liabilities
matching_rules:
  local_name_rules:
    - patterns:
        - "NoncurrentLiabilities"
      match_type: exact
      weight: 15
scoring:
  min_score: 15
