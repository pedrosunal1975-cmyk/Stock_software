# ESEF overlay: capital_expenditures
# Only fields that differ from base definition
component_id: capital_expenditures
matching_rules:
  local_name_rules:
    - patterns:
        - "PurchaseOfPropertyPlantAndEquipment"
      match_type: exact
      weight: 15
    - patterns:
        - "PurchaseOfPropertyPlantAndEquipment"
        - "AcquisitionOfPropertyPlantAndEquipment"
      match_type: contains
      weight: 10
  label_rules:
    - patterns:
        - "Purchase of Property, Plant and Equipment"
      match_type: exact
      case_sensitive: false
      weight: 20
scoring:
  min_score: 15
